name: Release Workflow

on:
  push:
    branches: [development, release-*]
  workflow_dispatch:
    inputs:
      promote:
        type: boolean
        default: false
        description: Promote release to stable (for release-* branches only)

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  id-token: write
  contents: write
  packages: write
  security-events: write

jobs:
  release:
    uses: epam/ai-dial-ci/.github/workflows/node_release.yml@4.3.1
    with:
      promote: ${{ github.event_name == 'workflow_dispatch' && inputs.promote }}
      node-version: 24
      maximize-build-space: false
      draft-stable-releases: false
      filter-version: '0\.[0-9]+'
      # Disable long running parts
      docker-build-enabled: false
      publish-enabled: false
      ort-enabled: false
      style-checks-enabled: false
      code-checks-enabled: false
    secrets: inherit
